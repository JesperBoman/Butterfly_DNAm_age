This is a directory for the [Nextflow methylseq pipeline](https://github.com/nf-core/methylseq) that I use to go from sequence reads (fastq-files) to methylation calls.

Step 1: I used cat_fastq.sh to combine technical replicate fastq files (sequenced on different lanes) of the same biological sample.

Step 2: Run pipeline with run_pipe.sh
